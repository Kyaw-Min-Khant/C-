using System;

namespace Day1
{
    class HelloWorld()
    {
        static void Day1(string[] args)
        {
            // string name = "Kyaw Kyaw";
            // int age = 21;
            // string is_single = "true";
            // bool single = Convert.ToBoolean(is_single);
            // char first_letter = 'K';
            // double salary = 1.5;
            // int new_salary = Convert.ToInt32(salary) * 100;
            // Console.WriteLine($"My name is {name} and age is {age}, My name first letter is {first_letter} and salary is ${salary},{is_single}");
            // Console.WriteLine(single.GetType());
            // Console.WriteLine(new_salary.GetType());

            // Console.WriteLine("What's your name ? ");
            // string name = Console.ReadLine();
            // Console.WriteLine("Please enter your age :");
            // int age = Convert.ToInt32(Console.ReadLine());
            // Console.WriteLine("Your name is " + name + " Age is " + age);


            // int num = 5;
            // num++;
            // num += 2;
            // num--;
            // num -= 1;
            // num *= 2;
            // num /= 2;
            // Console.WriteLine(num);
            // double x = 5.65;
            // double y = 4.5;
            // double a = Math.Pow(x, 3);
            // double b = Math.Sqrt(x);
            // double c = Math.Abs(x);
            // double d = Math.Round(x);
            // double e = Math.Ceiling(x);
            // double f = Math.Floor(x);
            // double g = Math.Max(x, y);
            // double h = Math.Min(x, y);
            // Console.WriteLine(g);
            // Console.WriteLine(h);
            // string name = "Kyaw Kyaw";
            // string phoneNumber = "123-456-789";
            // string validNumber = phoneNumber.Replace("-", "");
            // string validName = name.Substring(0, 4);
            // Console.WriteLine(name.ToUpper());
            // Console.WriteLine(name.ToLower());
            // Console.WriteLine(validNumber);
            // Console.WriteLine(validName);

        }
    }
}
